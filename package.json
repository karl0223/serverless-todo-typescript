{
  "name": "serverless-todo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "deploy": "serverless deploy",
    "offline": "npm run build && nodemon --watch src --ext ts,js,yml,json --exec \"serverless offline\"",
    "start": "serverless offline start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.3",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20.14.9",
    "nodemon": "^3.1.4",
    "serverless-offline": "^13.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2"
  }
}
